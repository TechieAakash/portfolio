# Portfolio Dashboard

A modern, interactive portfolio dashboard built with React and Vite.

## 🚀 Quick Start

### Prerequisites
- Node.js (v14 or higher)
- npm (comes with Node.js)

### Installation & Running

1. **Install dependencies** (first time only):
   ```bash
   npm install
   ```

2. **Start development server**:
   ```bash
   npm run dev
   ```
   
   The application will open at: **http://localhost:5173/**

### Other Commands

- **Build for production**:
  ```bash
  npm run build
  ```
  Creates optimized build in `dist/` folder

- **Preview production build**:
  ```bash
  npm run preview
  ```

- **Lint code**:
  ```bash
  npm run lint
  ```

## 📁 Project Structure

```
portfolio/
├── src/
│   ├── App.jsx       # Main portfolio dashboard component
│   ├── main.jsx      # React entry point
│   └── index.css     # Global styles
├── index.html        # HTML template
├── package.json      # Dependencies
└── vite.config.js    # Vite configuration
```

## ✨ Features

- **Overview Tab**: Animated metrics, activity charts, featured projects
- **Projects Tab**: Interactive project cards with detailed modals
- **Skills Tab**: Technical skills with progress bars and distribution charts
- **Fully Responsive**: Works on all screen sizes
- **Modern Design**: Gradient backgrounds, smooth animations, hover effects

## 🎨 Customization

Edit `src/App.jsx` to:
- Update personal information (name, links, avatar)
- Add your projects
- Modify skills and levels
- Customize colors and styling

## 📦 Dependencies

- React 19.2.0
- Vite 7.2.5 (rolldown)
- lucide-react (icons)

---

**Ready to use!** Just run `npm run dev` and start customizing.
